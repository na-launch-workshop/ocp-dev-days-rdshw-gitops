apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: claude-gateway
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: claude-gateway
    app.kubernetes.io/part-of: claude-sandbox
spec:
  to:
    kind: Service
    name: claude-gateway
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
  wildcardPolicy: None
